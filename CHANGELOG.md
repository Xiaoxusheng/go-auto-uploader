# 📝 Go Auto Uploader 迭代更新日志 (Changelog)

---
### [v1.3.1] - 最新更新
## 🚀 最新特性 (Features)

### 🖥️ 前端与交互体验 (UI/UX)

* **电竞级网络延迟监测 (Ping)**：为 WebSocket 实时信道新增了毫秒级的 Ping 值探测。前端每 5 秒发送一次时间戳探针，配合红、橙、绿三色动态预警，完美感知当前系统与浏览器的网络连通质量。
* **智能配置对比与富文本通知 (Rich Notifications)**：
* 重构了系统参数保存逻辑，新增**配置快照 (Snapshot)** 对比。修改配置后，右下角会弹出优雅的差异化清单（例：`🚀 并发数: 3 ➜ 5`）。
* 自动/手动触发扫描时，右下角会弹出包含“定时周期”、“并发线程数”及“挂载目录数”的数据化横幅提醒；扫描结束时会汇报本轮发现的新文件数量。


* **全局安全操作防护 (Safety Modals)**：为“清空队列”、“停止系统”、“重置远端”等敏感操作引入了带遮罩层的二次确认弹窗，针对高危指令（如“停止系统”）定制了专属警告语，彻底杜绝鼠标误触。
* **系统级视觉完善**：
* 引入了纯代码生成的 SVG `🚀` Base64 矢量图标 (Favicon)，解决了浏览器标签页空白不专业的问题。
* **全新空状态设计 (Empty States)**：为实时任务和历史记录表格设定了 `400px+` 的安全最小高度，并设计了带有巨型透明 Arco Icon 的居中占位图，解决了无数据时页面高度坍塌、排版跳动的问题。
* **终端日志自动追尾**：修复了日志刷新时的滚动逻辑，新日志产生后容器会自动 `scrollTop = scrollHeight` 滑动到最底部，高度还原真实的物理终端体验。



### ⚙️ 后端引擎与核心逻辑 (Backend)

* **全局日志拦截投射 (Global Log Interceptor)**：
* 舍弃了各处分散的硬编码推流，重构了底层标准库 `log.SetOutput`。
* 现在的引擎能够自动捕获所有物理控制台输出，剥离冗余的日期前缀，自动识别 `[ERR]` / `[WARN]` 关键字进行危险评级，并毫秒级投射到 Web 前端。


* **高并发反射探针 (WS Reflector)**：重构了 `handleWebSocket` 路由，加入了 `sync.Mutex` 互斥锁，支持无损、并发地处理并即刻原路弹回前端发送的 `ping` 消息。


## 🐞 修复与优化 (Bug Fixes & Optimizations)

* **修复目录监控数据停滞 (Stats Bug)**：解决了文件在“真实上传成功”或“哈希秒传成功”后，首页目录卡片中的【已上传文件数】和【上传量】始终为 0 的 Bug。现已加入 `updateSuccessStats` 钩子，数据即刻联动跳动。
* **修复并发状态显示脱节**：修复了控制台定期打印 `[QUEUE][STATUS]` 时，并发线程数永远显示启动时命令行的初始值的问题。现已改为动态加锁读取 `appConfig.Workers`，确保修改并发数后日志显示立即同步。
* **代码层级解耦**：清理了废弃的静态变量限制，统一由内存中的 `appConfigMu.RLock()` 统筹全权接管全局参数，使得并发、限速、扫描周期的“热更新”能力真正做到滴水不漏。

---

### [v1.3.2] - 最新更新

#### 🌟 核心特性 (New Features)

* **动态远端 API 配置 (Hot-Reload API Config)**：彻底告别硬编码！将远端服务器地址 (Endpoint)、认证账号、认证密码全部接入动态配置中心。现在可以直接在网页端“系统参数设置”中随时修改对接的服务器，保存即生效，无需重启或重新编译后端。
* **智能扫描溯源通知 (Smart Scan Triggers)**：重构了后端扫描指令下发逻辑。现在前端的扫描启动横幅能够智能识别并展示触发原因：
* `⏱️ 定时扫描执行中`（伴随显示当前设定的分钟周期）
* `▶️ 系统已恢复运行`（手动点击启动扫描时）
* `🔍 手动深度扫描启动`（手动强制重扫时）
* `⚙️ 配置重载扫描`（修改配置保存触发时）


* **电竞级网络延迟监测 (Ping/Pong)**：为 WebSocket 实时信道新增了毫秒级的 Ping 值探测。前端每 5 秒发送一次时间戳探针，配合红、橙、绿三色动态预警，完美感知当前系统与远端服务器的连通质量。

#### 🎨 界面与交互优化 (UI/UX)

* **高级数据网格 (Advanced Data Grids)**：
* 为“实时任务”与“历史记录”全面开启了专业表格特性：支持鼠标拖拽调整列宽 (`column-resizable`)、斑马纹 (`stripe`)、悬浮高亮 (`hoverable`) 以及全封闭边框 (`bordered`)。
* 优化了表格渲染引擎，移除了导致边框显示异常的虚拟列表限制，使列与列之间的纵向分割线更加清晰严谨。


* **纯净的时间感知暗黑模式 (Time-Based Dark Mode)**：引入了严格的昼夜作息自动切换逻辑。每天 `18:00` 至次日早上 `07:59` 打开面板将自动呈现极客暗黑模式，白天 (`08:00 - 17:59`) 则呈现清爽浅色模式。
* **全量中文化状态呈现 (Localization)**：在前端加入了状态转译拦截器，将底层的数据状态 (`success`, `uploading` 等) 在表格和筛选下拉框中无缝转译为优雅的中文（如“处理中”、“成功 (秒传)”），提升整体阅读体验。
* **全新空状态设计 (Empty States)**：为表格设定了 `400px+` 的安全最小高度，并设计了带有巨型透明图标的居中占位图（收件箱、打勾、历史时钟图标），彻底解决了无数据时页面高度坍塌、排版剧烈跳动的问题。
* **配置差异化快照 (Config Snapshot)**：保存配置时，右下角弹窗会动态计算并生成新老参数的“差异化对比清单”（例如：`🔗 API 地址已更新`，`🚀 并发数: 3 ➜ 5`）。

#### 🔧 底层与架构调整 (Backend & Architecture)

* **并发写锁升级 (WS Mutex)**：重构了底层的 WebSocket 处理器，将原本的读写锁升级为严格的普通互斥锁 (`sync.Mutex`)，确保在同时应对高频 Ping 探针反射与大批量日志广播时，绝对不会出现协程抢占导致的 Panic 崩溃。
* **全局日志拦截投射 (Global Log Interceptor)**：舍弃了各处分散的硬编码推流，通过接管标准库 `log.SetOutput`，自动捕获物理控制台的全部输出，剥离冗余时间前缀并识别 `[ERR]`/`[WARN]` 标签，毫秒级投射到前端虚拟终端。

---

### [v1.5.2] - 最新更新 (Live Radar & Smart Polling)

#### 🌟 核心特性 (New Features)

* **智能动态扫描调频引擎 (Dynamic Polling Scale)**：引入了“自适应变频”算法。系统不再死板地遵循固定扫描周期。当检测到当前有主播正在录制，或队列中有积压任务时，系统会自动切入**“狂暴模式”**（最高频每 3 分钟扫描一次）。当系统空闲时，会自动按比例降低扫描频率（每次递增 5 分钟），平滑过渡到最长休眠周期，极大地节省了硬盘 IO 性能与 CPU 资源。
* **直播录制雷达 (Live Radar)**：在“录制名单管理”面板新增了**实时状态侦测列**。后端通过静默扫描硬盘近期（3 分钟内）的文件写入波动，逆向推导并精准锁定当前正在开播的主播。前端页面会为其点亮醒目的 `🔴 正在录制` 呼吸灯徽章，无需翻看日志，当前战况一目了然！
* **录制引擎集成控制台 (Docker Integration)**：
* 系统参数中新增对录制引擎 Docker 容器名（如 `douyinliverecorder-app-1`）的绑定。
* 前端新增独立的引擎控制面板，可实时探测并展示容器运行状态（`🟢 Running` / `🔴 Exited`）。
* 支持在网页端一键执行 `启动引擎`、`重启引擎 (使配置生效)`、`停止录制` 操作。
* 引入了**“沉浸式实时日志窗”**，支持在网页端直接拉取 Docker 容器的最后 100 行物理日志输出，彻底告别 SSH 终端管理。


* **物理文件解析与热更新 (Config Hot-Write)**：实现了网页端对物理服务器上 `URL_config.ini`（录制名单）文件的直接穿透读写。用户可在网页直接添加直播间链接、无缝控制单个主播的监听开关（底层自动追加/剥离 `#` 符号）。

#### 🐞 修复与优化 (Bug Fixes & Optimizations)

* **“幽灵任务”强制剔除机制 (Ghost Task Killer)**：彻底修复了“上传失败后进度条卡死在处理中列表”的致命漏洞。现在，当网络断开或远端服务器返回非 200（拒绝接收）时，任务会被强制从 `uploading` 队列踢出并标记为失败记录。
* **热写入文件防占保护 (Hot-File Protection)**：重构了扫描器逻辑，遇到最后修改时间在 2 分钟内的文件，将判定为正在录制/拷贝中并静默跳过，避免因文件不完整导致的上传崩溃。
* **僵尸失败记录自愈 (Auto-Heal Failed Tasks)**：加入状态自愈钩子。如果某个文件因还在录制而产生过失败记录，只要它在后续扫描中最终上传（或秒传）成功，系统会自动反查并抹除它之前所有的失败黑历史。
* **邮件倒计时持久化 (Persistent Timer)**：修复了“点击保存配置会导致邮件倒计时重置，从而永远发不出邮件”的 Bug。现在计时器采用绝对时间锚点，无论你中途修改多少次系统参数，邮件必将雷打不动地准时发送。
* **BOM 幽灵字符深度清洗 (BOM Stripper)**：修复了 Windows 记事本编辑 `.ini` 文件时残留 `<feff>` 隐形字符导致链接识别损坏的问题。现在后端读写时会执行深度清理，确保写入的链接绝对纯净。
* **优雅的命名交接 (Smart Naming)**：针对 `douyin-live-recorder` 的特性进行了底层适配。如果在面板添加主播时不填名字，后端将只写入链接，把获取真实主播名称的工作交还给引擎，并在前端显示 `⏳ 等待引擎抓取...` 动画，抓取成功后前端会在 3 秒内自动轮询变更为真实姓名。
* **反刷屏进度控制台 (Anti-Spam Logs)**：重构了后端的上传进度打印逻辑。从原来的每秒疯狂输出，改为了智能的“10% 步长阶梯式打印”，完美解决了后台控制台和前端日志面板被几万条进度日志卡死的问题。

---
### [v1.6.0] - 最新更新 (Extreme Performance & Full-Duplex)

🌟 核心特性 (New Features)
全双工 WebSocket 广播引擎 (WS Broadcast Engine)：彻底终结了原先的高频 HTTP 短轮询（Polling）机制。后端新增独立的 wsDashboardBroadcaster 驻留协程，在有前端页面连接时，每 3 秒自动将全局“系统状态”与“队列数据”打包并通过 WebSocket 主动下推到所有客户端。

零网络开销时钟 (Zero-Overhead Clock)：将前端面板的本地“计时器跳动”引擎重构为纯粹的 1000ms 刻度驱动，完全剥离了原本捆绑的 HTTP 请求。这使得服务器后端 CPU 和高频网络 I/O 压力呈断崖式下降，面板操控体验实现了无感丝滑。

🎨 界面与交互优化 (UI/UX)
视觉风格统一 (Unified Glass UI)：去除了“录制引擎控制台”卡片左侧突兀的彩色边条，使所有控制面版严格对齐 Apple Glass 毛玻璃设计语言。

纯净胶囊开关 (Capsule Switch)：移除了表格中导致开关变为硬朗方形的 type="round" 强制属性，恢复了 Arco Design 原生灵动的全圆角胶囊形态，并内嵌了直观的对勾与叉号标识。

🐞 修复与优化 (Bug Fixes & Optimizations)
API 跨域与寻址修复 (CORS & BaseURL Fix)：修复了在触发 Docker 控制指令（如查看状态、启停容器）时，因将硬编码 IP 写死导致前端抛出 404 Not Found 和 CORS 跨域拦截的严重 Bug。现已全部收束规范为基于 Axios baseURL 的相对路由请求。

核心依赖补全 (Import Fix)：解决了上一版由于遗漏引入标准包 path/filepath，导致后端在编译“直播录制雷达”底层文件比对模块时抛出 undefined: filepath 的致命语法错误。

---

### [v1.6.1] - 最新更新 (Full-Duplex WS & API Refactoring)

#### 🌟 核心特性 (New Features)

* **全栈 WebSocket 双工通信接管 (Full-Duplex Takeover)**：完成了前后端通信架构的深度重构。彻底废弃了原有的 HTTP 轮询 (`setInterval`) 机制，将最高频、最消耗性能的两个大盘数据接口全面转移至 WebSocket 协议：
* `/api/v1/status` (系统状态、目录监控)
* `/api/v1/tasks/queue` (排队进度、饼图数据)


* **智能推流引擎 (Smart Broadcast Engine)**：后端新增独立的 `wsDashboardBroadcaster` 驻留协程。它能够智能感知前端面板的打开状态，仅在客户端在线时，每隔 3 秒自动将系统状态与队列数据打包下推，极大地降低了服务器的高频并发处理压力与无用的 HTTP 握手开销，实现了**零网络开销时钟**。
* **分级数据传输策略 (Tiered Data Strategy)**：确立了全新的 API 职责边界：
* **毫秒级/高频跳动数据**（上传进度、任务结算、探针延迟、系统大盘）：由 WebSocket 全权承载推流。
* **按需点击/重型查询数据**（历史翻页、日志拉取、Docker 容器控制与名单保存）：保留标准的 RESTful HTTP 接口，避免阻塞 WS 实时信道，保证核心上传任务流不受干扰。

---

### [v1.7.0] - 最新更新 (Ultra-Realtime & Cookie Engine)

#### 🌟 核心特性 (New Features)

* **全沉浸式实时终端 (Ultra-Realtime Logs)**：对“系统终端日志”进行了彻底的事件驱动重构。废弃了原有的 4 秒一次 HTTP 轮询 (`logPollingTimer`)。现在，当后端产生新日志的毫秒级瞬间，会直接通过 WebSocket 信道产生 `newLog` 事件单点突发推送至前端，真正实现了**零延迟**的真实物理机控制台体验。
* 面板新增独立的「Cookie 管理」标签页，支持在网页可视化填入抖音 (Douyin)、快手 (Kuaishou)、Sooplive 的录制抓取凭证。
* 后端独创了 **“无损 INI 精准覆写算法”**。告别粗暴的纯文本整体覆盖，后端现支持逐行扫描定位、精确切片替换对应的 `_cookie` 键值对（若无节点则智能智能追加），绝对保障引擎主配置 (`config.ini`) 中其他参数的安全与完整。


* **雷达与名单全量 WS 接管 (Streamer WS Broadcast)**：将“录制名单管理”页面中极度消耗性能的三大高频查询（Docker 容器运行状态、物理硬盘近期波动比对、`URL_config.ini` 文件解析）全部剥离 HTTP 请求，统一下放到后端的 3 秒 WebSocket 全局心跳广播中，进一步压榨并释放了服务器的性能极限。

#### 🎨 界面与交互优化 (UI/UX)

* **物理级终端滚动还原 (Native Terminal Scroll)**：重构了日志列表的渲染结构，去除了导致高度坍塌的过度动画，回归纯粹的定高 `overflow-y` 原生结构。配合新日志 `push` 追加机制，完美恢复了“最新日志永远在最底部，且滚动条自动向下追尾”的丝滑流畅体验。
* **跨设备协同热刷新 (Cross-Client Sync)**：引入了全新的 `streamersData` 双向数据流广播。当你在任何一台设备（如手机端）操作添加或修改了主播名单后，后端会通过 WS 信号即刻下推最新配置，其余所有保持网页打开的设备均会**无需刷新、全自动同步**最新画面。
* **优雅的异步占位符 (Graceful Placeholder)**：针对仅输入了链接、名字留空交由引擎获取的主播记录，优化了单元格的 `inline-flex` 横向对齐排版。添加了伴随旋转的 `<icon-sync spin />` 动效与“等待引擎抓取...”字样，彻底消除排版对不齐的毛糙感。

#### 🐞 修复与优化 (Bug Fixes & Optimizations)

* **斩断重度并发轮询瀑布 (Kill Concurrent Polling)**：彻底清除了前端隐患极大的 `streamerPollingTimer` 定时器。修复了此前只要用户切到“录制名单”标签，浏览器就会无脑每 3 秒向服务器并发轰炸 3 个独立 HTTP 请求导致 I/O 阻塞的严重性能漏洞。
* **结构体字段安全映射 (Struct Mapping Fix)**：修复了由于前后端变量首字母与命名规则不一致（前端 `recorderConfigPath` vs 后端错写的 `CookiesConfigPath`）导致录制引擎主配置文件物理路径永远无法被保存下来的底层级 Bug，现已完成字段归一化。
* **智能屏蔽保存闪烁 (Anti-Flicker Lock)**：在前端 WebSocket 的流数据接收口加入了写状态互斥锁 (`isSavingStreamers`)，以及基于 `JSON.stringify` 字符串缓存对比的防抖机制。从根源上杜绝了因网络回传时间差导致的页面 UI 无意义重绘和剧烈闪烁。


---

### [v1.7.1] - 最新更新 (Quick Search & Mobile Fixes)

#### 🌟 核心特性 (New Features)

* **极速主播检索 (Live Search)**：
    * 在“录制名单管理”页面新增了实时搜索栏。现在可以瞬间通过**主播名称**或**直播间链接**关键词过滤长列表，无需在数百个条目中肉眼查找。
    * **键盘党福利 (Hotkeys)**：引入了全局快捷键支持。在录制名单页面按下 `Ctrl + X` (Mac下为 Cmd+X) 即可一键聚焦搜索框，体验丝滑流畅。

#### 🐞 修复与优化 (Bug Fixes & Optimizations)

* **移动端响应式布局重构 (Mobile Layout Fix)**：
    * 彻底修复了在手机端/窄屏设备上，“录制引擎容器”控制栏右侧按钮（如“实时引擎日志”）被挤出屏幕不可见的问题。
    * 引入了弹性换行机制 (`flex-wrap`) 和更紧凑的间距策略，确保“启动/停止/日志”等核心操作按钮在任何屏幕尺寸下都完整可见且易于点击，同时自动隐藏不必要的文本以节约空间。
* **搜索结果联动统计**：现在搜索过滤后，右下角的统计栏会动态显示“🔍 搜索结果: N”，与总数区分开来，数据展示更严谨。


---
### [v1.7.2] - 最新更新 (Dashboard & Real-time Alerts)

#### 🌟 核心特性 (New Features)

* **动态可视化大盘 (Dynamic ECharts Dashboard)**：
  * 将系统首页从单纯的“工具面板”升级为具有数据洞察力的“监控大屏”。
  * 新增基于 ECharts 的 **7 日全站流量双 Y 轴面积趋势图**，同屏比对上传流量(GB)与处理文件数(个)的历史走势。
  * 新增 **TOP 5 硬盘空间消耗排行榜**，采用自适应比例进度条，直观揪出消耗本地存储与上行带宽的“流量杀手”主播。
* **全局实时强告警 (Real-time Alert Notifications)**：引入了系统级异常主动推送机制。当底层引擎遭遇远端 Token 失效、网络断开被拒绝、或物理目录访问异常等严重故障时，会立即通过 WebSocket 广播 `systemAlert` 信号。前端会瞬间在右下角弹出纯净风格的红色强提醒（无自动消失时间，强制用户干预），让你在看视频、做其他事时也能第一时间发现系统宕机。

#### 🎨 界面与交互优化 (UI/UX)

* **图表排版自适应修复 (Chart Layout Fix)**：修复了前端 ECharts 趋势图底部图例 (`legend`) 掉落并与 X 轴日期严重重叠的布局溢出问题。优化了网格 (`grid`) 安全边距，并微调了任务分布饼图的位置，使全站所有图表在各种分辨率下都保持舒展与优雅。
* **纯净版官方通知 (Pure Notifications)**：剥离了之前覆盖在 Arco Design `Notification` 组件上的冗余自定义 CSS，恢复了框架原生的极简高级感，并开启了 `white-space: pre-wrap` 支持后端传回的多行报错日志完美换行。

#### 🔧 底层与架构调整 (Backend & Architecture)

* **双工通信全面接管 (Full-Duplex Analytics)**：彻底废弃了原先规划的短轮询 HTTP 接口。将大盘聚合数据 (`statsTrend`) 与瞬时上行速率 (`trafficMetrics`) 全面改造为 WebSocket 驻留协程驱动。服务端每 2 秒静默计算并按需下推，前端配合 `JSON.stringify` 数据指纹防抖，实现了“零网络开销”的丝滑大屏刷新。
* **数据持久化策略重构 (Data Persistence)**：移除了旧版中“每次发送 SMTP 汇总邮件后强行清空本地成功日志 (`upload_success.json`)”的粗暴逻辑。将日志转为持久化累积，为 7 日趋势图提供了稳定可靠的数据源基石。

#### 🐞 修复与优化 (Bug Fixes & Optimizations)

* **断电/重启状态自愈 (State Recovery)**：修复了系统每次重启或断电更新后，“整体任务分布”饼图中“已成功”队列数据全部归零的致命视觉缺陷。底层新增了 `restoreQueueCounts` 生命周期钩子，在系统初始化启动时会自动从本地 JSON 成功日志中反向注水恢复成功记录数，确保大屏数据永久连贯。
---

### [v1.8.0] - 最新更新 (Dual-Engine Hot-Reload & WS Deadlock Fix)

####  🌟 核心特性 (New Features)
* **内置引擎底层热重载 (Built-in Hot-Reload)**：实现了内置轻量录制引擎的底层配置文件 (`builtin_urls.txt`) 热重载。通过 SSH 或外部修改该文件（增删行、加 `#` 暂停），引擎将在 3 秒内自动感知识别，并无缝同步至所有在线前端设备的屏幕上，彻底与外置 Docker 引擎体验看齐。
* **主播名称自动固化 (Auto-Pinning)**：修复了内置引擎重启后“失忆”的问题。现在即使目标未开播，只要引擎成功抓取到主播网页中的名称，就会偷偷将其反向写入底层的 `builtin_urls.txt` 进行永久固化保存，下次开机秒出名称。
* **瞬间中断唤醒机制 (Instant Wake-up)**：摒弃了之前扫描系统“被动等待周期”的漏洞策略。现在，只要内置引擎捕获到主播开播并切入“录制中”状态，会精准延迟 2.5 秒后主动发射硬件级中断信号，“一脚踹醒”主程序的休眠定时器，瞬间进入狂暴扫描模式！
* **封面图原生下载 (Cover Downloader)**：为前端监控表格的直播画面新增了原生下载按钮，采用 `Blob` 二进制流拦截技术，点击即可一键将高清直播封面保存到本地（以主播名与时间戳自动命名）。

#### 🎨 界面与交互优化 (UI/UX)
* **苹果 Safari 深度防溢出重构 (iOS Responsive Fix)**：
  * 彻底解决了在 iPhone 等窄屏设备上，表格列宽超出屏幕导致整个网页被“撑破”、左右乱晃的致命排版 Bug。
  * 引入 `max-width: 100vw; overflow-x: hidden;` 全局锁，并采用 Arco 原生 `:scroll="{ x: 800 }"` 开启 Safari 专属硬件加速横向阻尼滚动。
  * 强制所有输入框字体大小不低于 `16px`，完美免疫 iOS 点击输入框自动放大页面的系统级缺陷。
  * 引入 `viewport-fit=cover` 与 `env(safe-area-inset-*)` 变量，全面适配全面屏手机的刘海屏与底部横条防遮挡。

#### 🔧 底层与架构调整 (Backend & Architecture)
* **彻底根除 WebSocket 阻塞假死 (WS Deadlock Fix)**：重构了底层的 WebSocket 并发锁模型。将原先的“全局大锁”拆分为基于 `WSClient` 结构体的独立读写小锁。彻底解决了因为某个网页刷新产生“半开幽灵断连”而导致底层 I/O 阻塞、进而引起新打开页面死活刷不出数据的严重假死问题。
* **FFmpeg 刷屏日志屏蔽 (Log Clean-up)**：切断了 `ffmpeg` 下载进度条对系统标准输出的绑定。解决了作为 `systemd` 服务运行时，控制台被疯狂刷新的回车符导致产生大量 `[xxxB blob data]` 二进制乱码折叠的问题，还系统日志一片清净。
* **Soop 平台智能风控拦截 (Anti-Ban Catch)**：优化了内置引擎对 Soop 平台的解析逻辑。当遭遇 Cookie 失效或 19+ 登录墙拦截时，不再抛出无意义的 `< (JSON 解析失败)` 乱码报错，而是精准拦截 HTML 响应并输出 `被平台风控拦截(需更新Cookie)` 的强提示。
* **前端空值安全校验 (Safe Filter Check)**：修复了搜索框输入过快或遇到数据包为 `null` 时，Vue 计算属性 `toLowerCase()` 报错导致整个搜索栏状态被强制重置清空的隐蔽 Bug。
---
## 📜 文档与规范 (Documentation)

* **标准化开源库主页**：起草了带有特性说明、启动参数表、API 规范以及带占位符的 6 图展示位的标准企业级 `README.md`。
* **开源协议合规**：引入了极度宽松友好的 `MIT License` 声明，为项目的后续传播与二次开发打下基础。