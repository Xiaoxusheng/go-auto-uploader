# 高光切片训练 —— 总计划与进展

> ⚠️ **每次工作结束后必须更新本文件**（用户约定，2026-09-23）。
> 技术细节见 `highlight-accuracy-plan.md`；采集实例说明见 `_diag/live_train/README.md`。
> 本文件只回答三个问题：**现在在哪、结论是什么、下一步做什么。**

**最后更新**：2026-09-23 15:38

**本文件由多个 agent 共同维护。更新时请只改自己确认过的部分，并保留别处的结论。**

---

## 0. 一句话现状

现状算法在**真留一切片**下的 F1 只有 **0.584**（短板是查全率 0.437）。
已落地一个**零代码改动**（三个配置数字）把它提到 **0.717（+23%）**。
**当前卡在「训练素材」上**：31 个已录切片的三条判据达标率只有 **4/31（12.9%）**，
其中 1 个是已排除的成人内容、2 个是**存疑的 `梦梦m`**（见 §4.5）——
**真正干净可用的仍只有 `caicai-003` 一条。**

**下一步**：按「**粗筛 + 看图**」流程筛边缘案例（3/4、2/4 的切片），
把够格的标注进训练集；同时保证采集实例**长期运行**（见 §2 运维说明）。

**✅ 已突破**：找到形态正确的素材（`菜菜很忙` 直播，固定机位 + 舞蹈/常态分层），
加入训练集后 **tuned（主播历史基线）F1 = 0.779**，首次超过纯配置改动的 0.717，
**并且首次验证了「主播历史基线」确实有效**。详见 §4.7。

**下一步**：继续按 4 条判据采集/筛选（重点：**固定机位**），
攒到 15~20 条后再考虑判别模型。

---

## 4.7 ✅ 突破：素材形态对了，主播历史基线首次生效

### 关键素材：`菜菜很忙🦋` 的直播切片

15 帧概览确认它**完全符合 4 条判据**，而且是 `caicai-003` 的**同主播**：

- **全程固定机位**（女仆装、全身入镜、背景固定）← 关键，避开了 `笛笛畏` 的机位问题
- 有**站立轻摆**（常态）与**舞蹈/摆姿势**（转身、抬腿、手臂动作）
- 竖屏、单画面、真人实时

判据：`p90 24.0 / 对比度 3.01 / z 峰值 3.20` → **4/4 全过**。

标注（经抽帧确认）：`[[65, 286], [829, 900]]`。

### 训练结果：全线上涨

| 配置 | 10 切片（旧） | **11 切片（含菜菜很忙）** |
| --- | --- | --- |
| baseline（切片内 + 0.8/0.2 + th1.5） | 0.516 | **0.662** |
| **tuned（主播历史基线）** | 0.645 | **0.779** ← 最高 |
| model（逻辑回归 5 特征） | 0.554 | **0.722** |

**两个重要结论**：

1. **素材形态是决定性的**。`笛笛畏`（机位切换）让指标**掉到 0.516**；
   `菜菜很忙`（固定机位）让指标**涨到 0.779**。同样的算法、同样的流程，
   **只差「机位是否固定」这一条**。
2. **「主播历史基线」终于生效了**（§4.2 里因缺数据无法验证的那条）。
   现在有两条同主播切片（caicai-003 + 菜菜很忙直播），
   tuned（0.779）显著优于 baseline（0.662）—— 差 0.117 全部来自归一化方式。

⚠️ 注意：tuned 的最优超参是 `0.75 / +0.05 / th0.84`，与线上配置（`1.0 / 0.0 / 1.2`）
不同。**但 11 个切片的留一结果仍不足以定超参**，需要更多数据。

---

---

## 4.6 ⚠️ 重大发现：运动量特征对「机位切换」系统性失效

**原以为找到了第二条可用素材 `笛笛畏`**（近景说话 vs 站起来跳舞，形态与 caicai-003 一致），
标注后加入训练集，结果**所有指标全线下滑**：baseline F1 `0.584 → 0.516`，
而笛笛畏自己 F1 只有 **0.346**。

**查运动量分布，发现它与标注几乎负相关：**

| 时段 | z 均值 | 人工标注 | |
| --- | --- | --- | --- |
| 0-40s | **−0.71 / −0.89**（全片最低） | 高光 | ❌ 矛盾 |
| 120-140s | **+0.79** | 常态 | ❌ 矛盾 |
| 200-220s | **+1.00**（全片最高） | 高光 | |

**根因：近景说话时脸占满画面，帧差很大；站起来跳舞时切远机位、人物变小，帧差反而小。**

所以运动量度量的是「**画面变化量**」，不是「**动作幅度**」。
**算法会系统性漏检「切远机位的舞蹈」，同时误检「近景说话」。**

**这解释了为什么 `caicai-003` 能到 F1 0.96 —— 它全程固定机位**，
跳舞和聊天都在同一距离，运动量才真正反映动作幅度。

### 结论与行动

1. **`笛笛畏` 的标注已撤回**（它与特征负相关，会污染训练集）。标注数回到 10。
2. **素材合格判据必须增加第 4 条：机位固定**（不切远近景）。
   这条**无法用现有特征自动判断**，只能看图 —— 抽帧时一眼就能看出来。
3. **采集侧应优先找「固定机位」的主播**（单机位、不切景别）。
4. 这也说明：**「高光」的语义定义（§4.5）与运动量特征之间存在系统性鸿沟**，
   不是调参能解决的。要跨越它，需要**对机位/尺度不敏感的特征**
   （如人物框内的相对运动），那是另一个量级的工程。

---

## 0.1 ⚠️ 采集实例的运维（必须知道）

**实测：Bash 会话后台任务启动的实例会被回收，且回收时长完全不可预测** —— 进程、端口、
ffmpeg 全部消失，**采集静默中断、无任何报错**。三次实测：30 分钟（15:14→15:44）、
68 分钟、**4 小时 31 分（15:58→20:29，2026-09-23 本次最长）**。所以**不能把它当常驻服务**，
必须按下面的方式跑，并且每次都假设它随时会停。

**⚠️ 2026-09-23 17:00 更正：`explorer.exe start.bat` 那条路已经失效，别再用。**

早前记的"进程挂到 explorer 名下、能脱离会话"**不再成立**：现在 `explorer.exe <*.bat>`
返回 exit=1 且**批处理根本不执行**（日志不轮转、无进程），即使 `dangerouslyDisableSandbox`
也一样 —— 沙箱会拦掉 explorer 派生出的 cmd。当时的存活记录应是旧沙箱策略下的巧合。

**agent 侧启动进程的真实约束（实测矩阵）**

| 方式 | 结果 |
| --- | --- |
| `explorer.exe <*.bat>` | 静默失败，exit=1，**不执行** |
| `cmd.exe /c`（含放开沙箱） | 直接拒绝："bypasses all command validation" |
| `wscript` / `cscript` / `schtasks` | Program Blacklist，直接拒绝 |
| PowerShell `Start-Process` | PS 5.1 建大小写不敏感环境字典撞 `http_proxy`/`HTTP_PROXY`；起得来也被回收 |
| `set_flags` 无关。**`python launch_detached.py`** | ✅ 能起（`DETACHED_PROCESS`+`CREATE_NEW_PROCESS_GROUP`），但**命令结束即被 SIGTERM**（日志可见 `received signal 15`），`CREATE_BREAKAWAY_FROM_JOB` 被 job 拒绝（WinError 5） |
| Bash 工具 `run_in_background` | ✅ 立刻可录，**回收时长不可预测**（实测 30min / 68min / 4h31min） |

**结论：agent 无法拉起一个真正长命的实例** —— Bash/PowerShell 两条通道的工具宿主都用
job object（未开 breakaway）连带回收整棵进程树。所以：

1. **长期跑：人工双击 `_diag/live_train/start.bat`**（cmd 内建 `start /min`，脱离工具宿主）。
   这是目前**唯一**能长期存活的路径，agent 会主动提示用户点。
2. agent 需要立刻恢复采集时：`python launch_detached.py --port 18080 --rotate-log`
   （脚本在 `_diag/live_train/`），先把录制接上，再让用户双击接管。
   代价是**约 68 分钟后静默中断**，必须配巡检 —— 别以为它一直在录。

重启时若报 `bind: Only one usage of each socket address`，是旧实例的 TIME_WAIT，
**等 10~20 秒再启动**即可。被杀时 ffmpeg 可能变成孤儿进程继续写 TS，重启前
`tasklist | grep ffmpeg` 清一遍。

**停止实例**：进程**没有控制台窗口**，所以 `taskkill /PID`（不带 `/F`）会被拒
（"只能强行终止这个进程"）；且 Git Bash 里 `//PID` 会被路径转换吃掉（报"无效参数 - '//PID'"）。
正确写法：

```bash
PID=$(netstat -ano | grep ":18080" | grep -i listening | awk '{print $NF}')
MSYS_NO_PATHCONV=1 taskkill /F /PID "$PID"
```

`stop.bat` 内部同款逻辑（按端口反查 PID，不会误伤别处的 uploader.exe）。

> 2026-09-23 20:39 用户决定**停掉**采集实例：agent 起的实例当天三次被回收（4h31m / 5m38s / 2m），
> 保活成本高于收益。**当前状态 = 不录制**，重新开启靠双击 `start.bat`。

---

## 4.5 ⚠️ 核心问题：「高光」的定义是什么（未决）

达标率统计暴露出一个**判据无法回答的问题**：

| 素材 | 运动量中位 | p90 | z 峰值 | 自动判定 | 看图结论 |
| --- | --- | --- | --- | --- | --- |
| caicai-003 | 6.9 | 21.9 | 4.07 | ✅ | 跳舞 vs 聊天，**语义不同** |
| 梦梦m | 11.1 | 24.1 | 4.65 | ✅ | **都在跳舞**，只是幅度有快慢 |
| dance-a3d6 | 11.1 | 31.8 | 4.11 | ✅ | ⛔ 成人内容，已排除 |

**`梦梦m` 运动量确实有分层**（中位 11 vs p90 24，差 2 倍），所以自动判据放它过。
但看图，它**全程都在挥绸带跳舞** —— 只是有时挥得大、有时小。

**两种可能的「高光」定义，导致完全不同的结论：**

- **定义 A（运动量大）**：`梦梦m` 可用，判据正确，12.9% 就是可用率
- **定义 B（视觉/语义上突出，如「跳舞 vs 聊天」）**：`梦梦m` 不可用，
  判据只能**粗筛**，最终必须看图 —— 实际可用率**远低于 12.9%**

**`caicai-003` 的标注走的是定义 B**（原文：「437–645 秒为跳舞，其余为摆姿势/聊天/待机」）。
而**运动量特征只能捕捉「幅度」，捕捉不到「语义」** —— 跳舞和站着聊天在运动量上可能接近，
大幅跳舞和小幅跳舞在语义上却是同一类。

**这个定义决定了整个训练目标的上限**，必须先定，否则后面所有工作都可能白做。

---

## 1. 目标与验收

| 层级 | 指标 | 当前 | 目标 |
| --- | --- | --- | --- |
| 技术（训练用） | 秒级 F1（真留一切片） | 0.584 → **0.717**（已落地） | ≥ 0.80 |
| 技术（验收用） | 段级 P/R | 未测 | P ≥ 0.8 且 R ≥ 0.6 |
| 业务 | 高光片段可用率 | 未测 | 待定 |

**反例基线**：现状 F1 **0.584** —— 任何改动都必须超过它。

---

## 2. 当前状态速查

| 项 | 状态 |
| --- | --- |
| 标注切片 | **10 个**（caicai-003 + SOOP 6 + golani/gorani 3） |
| 特征缓存 | 31 个切片已提特征（`_diag/train/cache/`） |
| 可用训练数据 | ⚠️ **只有 caicai-003 一个够格** |
| 采集实例 | ✅ 运行中（`http://127.0.0.1:18080`，4 路在录）。**切片已修**（见 §10.1）、**磁盘有守护**（§10.2）、**每小时自动入库**（§10.2） |
| 线上配置 | ⚠️ **仍是旧值 `0.8/0.2/th1.5`，未部署**（`/home/upload/config.json`）。但 **SSH 已通**，随时可部署（§6） |

---

## 3. 路线图与进度

| Phase | 内容 | 状态 |
| --- | --- | --- |
| 0 | 让准确率可测量 | ✅ 完成（10 切片 + `hleval` + 6 个评估脚本） |
| 1 | 特征与归一化升级 | ✅ 已实证 → **结论：不改归一化**（§4.2） |
| 2 | 学参数 | ✅ 完成 → **纯配置改动收益最大**（§4.3） |
| 3 | 判别模型 | ⏸ 暂停（逻辑回归泛化反而更差，且数据不足） |
| 4 | 数据闭环 | 🔄 **进行中**（采集实例 + 自动筛） |

---

## 4. 已确认的结论

### 4.1 现状基线（真留一切片，10 切片 / 3875 行）

| 配置 | P | R | F1 |
| --- | --- | --- | --- |
| 现状（切片内归一化 + 0.8/0.2 + th1.5） | 0.879 | 0.437 | **0.584** |
| **落地候选（1.0 / 0.0 / th1.2）** | 0.778 | 0.665 | **0.717** |

**短板是查全率**（漏检一半以上高光），不是误检。

### 4.2 归一化：不值得改

6 个方案真留一对比，最好的「位置收缩」也只比现状好 **0.022**（0.717 vs 0.695）。
而且「全局位置」类方案**更差**（0.447~0.482）—— 因为全局中位数被高量级主播拉高，
给低量级主播定标会让 z 全变负。

### 4.3 音频权重应该为 0

三条独立证据一致（逐列 AUC 排名 / 超参搜索 / 回归系数为负）：
运动量单独 AUC = **1.000**，加 0.2 音频权重反而降到 0.999，纯音频 AUC 仅 **0.197**。

### 4.4 最划算的改动是纯配置（已落地）

```json
"highlight_motion_weight": 1.0,   // 原 0.8
"highlight_audio_weight": 0.0,    // 原 0.2
"highlight_threshold": 1.2        // 原 1.5（F1 峰值）
```

⚠️ 两个权重**必须同时给**：回落条件是「两者同时为 0」，
只写 `audio_weight:0` 会被静默改回 0.8/0.2。已有测试钉住这个契约。

---

## 5. 被推翻的假设（最有价值的部分）

| 假设 | 实测结果 | 教训 |
| --- | --- | --- |
| 「主播历史基线」能修缺陷 A | ❌ 几乎无贡献（8/10 冷启动） | 依赖历史的方法，在没历史时等于没修 |
| 「全局位置 + 切片内尺度」能修缺陷 A | ❌ **最差之一**（F1 0.482） | 凭直觉提假设前，先看数据形态 |
| `v_YLOW/v_YAVG` 能测「整屏均匀变化」 | ❌ 恒为 0 | 160x90 缩略图里静止像素永远 ≥10% |
| 场景切换是「针状尖峰」，中值滤波能压 | ❌ 是**持续十几秒的段**，滤波后仍 14.02 | 同上 |
| `p90/median` 能筛出好素材 | ❌ 有分母陷阱（FISH 4.60 但无高光） | 比值类指标必须配绝对量门槛 |
| 逻辑回归能提升泛化 | ❌ 反而更差（0.604，P 仅 0.495） | 系数在不同素材上符号会翻转 |

---

## 6. 待办与下一步

**立刻可做**
1. 统计 31 个切片的三条判据达标率 → 判断瓶颈性质
2. `菜菜很忙🦋` 录满后提特征（caicai-003 同主播，能验证主播历史基线）
3. 对半枝桃等切片做标注（目前只有 caicai-003 一条）

**中期**
4. 采集实例持续跑，攒「有聊天/待机间隔」的素材
5. 素材够了（15~20 条）再考虑 Phase 3 判别模型

**阻塞项**

6. ~~线上部署（SSH 不通）~~ → ✅ **SSH 已通，阻塞解除**（2026-09-23 15:24 实测）

   ```bash
   ssh xyx.homes "hostname"     # → localhost.localdomain  ✅
   ssh 192.168.5.10 "hostname"  # → Connection closed      ❌ 别用 IP
   ```

   走域名可以，是因为 Clash Verge 的 WARP-AUTO 提供了 IPv6 出站（域名只有 AAAA 记录）。
   注意本机 DNS 被 Clash 全量劫持成 fake-ip（连 `nslookup ... 223.5.5.5` 都返回 `198.18.x.x`），
   所以**永远不要用解析出来的 IP 去连，直接写域名**。

   **线上目标**：`/home/upload/uploader`，systemd 单元 `uploader.service`
   （`WorkingDirectory=/home/upload`，`Restart=always`，`ExecStart` 带 `-web-port=8888`）。
   同机还有一堆别的服务（EchoIM 全家桶、myblog、openlist、`douyinliverecorder-app-1`）——
   **那个 `douyinliverecorder` 容器是第三方 ihmily 录播器，跟本项目无关，别动它。**

   **线上高光参数仍是旧值**（`/home/upload/config.json`）：
   `highlight_enable: true` / `motion 0.8` / `audio 0.2` / `threshold 1.5`
   → **§4.4 那三个数字还没上线**，线上仍在用 F1 0.584 那套。

   **两种生效方式**（配置**不热重载**：`AppCfg()` 读的是内存里的 `CfgStore`，全仓没有 fsnotify，
   所以**直接改 config.json 文件不会生效**）：

   | 方式 | 做法 | 代价 |
   | --- | --- | --- |
   | A 控制台/API | `PUT /api/v1/config`（整份配置，走 RSA+AES 加密提交），保存即热生效 | 需控制台口令，无中断 ✅ |
   | B 改文件+重启 | 改 `/home/upload/config.json` 后 `systemctl restart uploader` | **打断所有在录会话**，重启期间丢帧 |

   **⚠️ 部署前要注意水印口径不一致**：线上 `watermark_enable: true`，
   而训练素材是 `false`（采集实例刻意关掉，见 `_diag/live_train/README.md`）。
   线上水印带动态时钟（`watermark_format` 含 `%H:%M:%S`），会往 `signalstats` 里
   注入一份每秒变化的固定噪声。建议先确认影响，或上线后抽查几条产出再全量。
   **这部分还只是推断，没有实测数字，不要当作已验证结论。**

---

## 7. 素材合格判据

**三条自动判据（粗筛用，`screen_clips.py` 输出 n/4）**

| 判据 | 含义 | 反例 |
| --- | --- | --- |
| `p90 > 20` | 有真高光（大幅动作） | FISH 11.6（全程静坐） |
| `对比度 > 2` | 有常态/高光分层 | SOOP 系列 1.4~1.6（整片都在动） |
| `z 峰值 > 3` | 分层**足够强**（**参考值，不是硬门槛**） | 笛笛畏 2.75（但形态完美，不该被丢） |

**第 4 条必须看图判断（无法自动检测）**

| 判据 | 含义 | 反例 |
| --- | --- | --- |
| **机位固定** | 不切远近景 | 笛笛畏：近景说话 vs 远机位跳舞 → **运动量与语义负相关** |
| 单画面 | 不是「主播 + 背景视频」双路 | Mini kua / Miller 米乐（画中画） |

**标杆**：`caicai-003` = p90 21.9 / 对比度 3.19 / z 峰值 4.07 / **全程固定机位**。
目前**唯一**全部满足的素材。

⚠️ **自动判据只能粗筛**：它们都是运动量指标，回答不了「语义上算不算高光」，
也**检测不出机位切换**（§4.6）。必须配合看图。

---

## 8. 资产清单

| 位置 | 内容 |
| --- | --- |
| `docs/highlight-accuracy-plan.md` | 技术方案 + 全部实证数据（§11 是实测章节） |
| `tools/train_highlight/` | `train.py` / `ablate_norm.py` / `norm_variants.py` / `label_assist.py` / `screen_clips.py` / `scene_switch_probe.py` |
| `cmd/hleval/` + `internal/highlight/features.go` | 50 列特征提取（独立二进制，不进主链路） |
| `_diag/train/` | `cache/` 特征缓存、`sheets_live/` 拼图、`labels.jsonl` 标注 |
| `_diag/live_train/` | 采集实例（配置/脚本/README/tick.sh） |
| `_diag/hlcfgcheck/` | 打印配置生效值 |

---

## 9. 已知缺陷（未修）

| 缺陷 | 说明 | 状态 |
| --- | --- | --- |
| A | 单切片归一化 → 整片都是高光时必然漏检 | 修法已验证**无效**，暂不修 |
| B | 音频权重方向反了 | ✅ 已修（配置改 0） |
| C | 只有 2 维特征（现已扩到 50 列） | ✅ 已扩 |
| **D** | **纯运动量把「整屏像素同时变化」误判成高光**。两个独立实例：① 游戏菜单切页（z=**17.46**，是真实跳舞 4.07 的 4 倍）② **礼物/贴纸全屏特效**（FISH，z=**8.39**，画面是全屏小狗动画） | ⏸ 两种简单修法都失败，需空间分块（中等改动，未验证）。⚠️ 实例②比①更值得修：**礼物特效在所有抖音直播间都会出现**，频率远高于游戏切页 |
| **E** | **运动量对「机位切换」系统性失效** —— 近景说话脸占满画面帧差大、远机位跳舞人物变小帧差小，**与语义负相关** | ⏸ §4.6 实证（笛笛畏）。不是调参能解决的，需要**对尺度不敏感的特征**（人物框内相对运动），是另一个量级的工程 |

---

## 10. 另一路 agent 的改动（2026-09-23 15:14–15:35）

> 与 §4.6 那轮是**并行**工作，结论不冲突。§4.6 是特征层面的发现，本节是工程与验证层面的。

### 10.1 ✅ 修复：`segment_time` 单位错了 —— 录制**从未切过片**

配置写的是 `300`，但 `SegmentTime` 的单位是**分钟**（`internal/recorder/builtin_ffmpeg.go:362`
是 `-segment_time effectiveSegment*60`），所以实际等于 **5 小时**。
`_diag/live_train/README.md` 里"5 分钟一片"那句是错的（已改）。

**实证**：`小北_2026-09-23_14-28-52_000.ts` 从 14:28 一直写到 15:05（36 分钟 / 583MB），
全库**没有任何 `_001` 文件**。

**影响比"文件大"严重**：`collect.py` 要求 `mtime` 静置 120 秒才处理 → 长切片永远在处理队列外
→ **素材入库延迟 = 整场时长**（主播播 6 小时就要等 6 小时），今天最该处理的那批永远排在最后。

已把值改为 **15**（≈ `caicai-003` 的 899 秒，足够含一次"聊天→才艺"的状态切换）。
**修后验证**（直接读 ffmpeg 进程命令行，不是看配置）：
4 路全部 `-f segment -segment_time 900 -reset_timestamps 1 ..._%03d.ts`。

### 10.2 ✅ 新增：磁盘守护 + 每小时自动入库

之前**没有任何东西在防止写满盘**：D 盘剩 92GB、实测写入 **3.6GB/小时** → 约 **25 小时**见底。

- `_diag/live_train/retain.py`：素材分三类（🔒 受保护 / 👀 待人工复核 / ♻️ 可回收），
  默认**只报告**；`--guard` 在空闲 < 20GB 时**停采集而不是删数据**；只有 `--reclaim --yes` 才真删。
- `_diag/live_train/protect.txt`：手工保护名单（아란=缺陷 D 取证 / FISH=礼物特效取证 /
  燃燃长片 / 菜菜很忙\* / 梦梦m 长片）。
- 已挂**每小时定时任务**：跑 `tick.sh` 补入库 + `retain.py --guard`。
  （`tick.sh` 本来就按"可挂定时任务"设计，只是一直没人挂 —— 这是入库延迟的另一半原因。）

### 10.3 ✅ 独立验证：0.717 经得起「剔除自证标签」

`labels.jsonl` 有 3 条 note 写着"候选段标注（未逐帧人工复核）"，而方案 §7 自己告诫过
"候选段不是标注"。剔掉这 3 条重跑（7 片 / 3228 行；先复现了 §11.7 全部数字以确认环境一致）：

| 配置 | 全量 10 片 | 仅 7 片人工复核 |
| --- | --- | --- |
| 现状 `0.8/0.2 th1.5` | 0.584 | **0.622** |
| B 主播历史基线 | 0.614 | 0.654 |
| C 折内搜（切片内归一化） | 0.704 | 0.709 |
| E 逻辑回归 | 0.604 | 0.627 |
| **落地候选 `1.0/0.0 th1.2`** | 0.717 | **0.729** |

**结论：那 3 条自证标签不是在撑数字，反而在压低它** —— 去掉后每个数字都上升。
§4.4 的三个数字可以放心采纳，**相对提升修正为 +17%**（0.622 → 0.729，基数变了）。

顺带两条：

- ⚠️ **`v_YLOW` 应该划掉**：折内系数两次都是**负**的（−0.22 / −0.24），
  而 §11.5.1 在单条素材上量到 **+2.84**。**§11.5.2 第三条不要执行**。
- 阈值最优仍稳在 **th≈1.2**，与 §11.9 一致。
- 详见 `_diag/train/VERIFY_2026-09-23.md`。

### 10.4 新素材筛查（10 片，逐片看图）

见 `_diag/train/SCREEN_2026-09-23.md`。**新批次 10 片没有一片达到 `caicai-003` 的形态**，
失败原因分三类，值得记下来因为三类要三种不同的对策：

| 类型 | 例子 | 对策 |
| --- | --- | --- |
| 真才艺但**无状态边界**（常态段也在动） | 乔乔吃不饱 / 半枝桃 / 7末 | 换主播形态，不是换判据 |
| **有边界但没高光** | FISH（常态=坐着说话，候选=撩头发/喝水/礼物特效） | 判据要抓 |
| **假运动** | yuan（手机机位移动）/ Mini kua（双人连麦）/ 梦梦m（全程动作） | 见 §7 第 4 条 |

**对 §7 判据的两条补充建议**（都来自这轮看图）：

1. **补一条「中位运动量必须落在 [4, 12]」**。`robustZ` 是切片内自适应的，所以
   **中位太低 → z 被放大**：FISH 中位只有 **2.5**，"撩头发/说话"这种画面变化就拿到 **z=4.26**，
   9 个"候选"没一个是才艺。这与 §9 缺陷 A（中位太高 → z 被压平）是**同一行代码的两个方向**，
   合起来的推论是 **z 分不能单独当判据，绝对量级必须一起进**。
2. **加一个「候选密度（个/分钟）」参考值**：`caicai-003` = **0.07**，
   而 FISH 1.03 / golani-2 1.22 / 梦梦m 0.67 全都高一个数量级。
   密度高 = "高光"不孤立 = 没有真正的常态参照。

⚠️ 本轮结论都建立在"逐片看图"上，样本是**修 `segment_time` 之前**录的那批（多为短切片），
修好之后 15 分钟切片更容易含状态切换，可用率需要重新测。

---

## 11. 用「跳舞高光」口径做实：4 条新标注 + F1 0.717 → 0.831

> 用户已明确任务定义 = **跳舞高光**（不是泛指"大动作"）。本节按这个口径做的。
> 与 §10 同一路，但结论更进一层。

### 11.1 新增 4 条标注（全部固定机位 + 抽帧核对）

`菜菜很忙🦋`（= `caicai-003` 同主播）今天在播，是唯一确认可用的形态，优先标了她：

| clip（cache 名） | 秒 | 中位 | p90 | 对比度 | z峰值 | positive |
| --- | --- | --- | --- | --- | --- | --- |
| `..._15-14-26_000` | 901 | 7.97 | 23.95 | 3.01 | 3.20 | 65–286 / 829–900 |
| `..._14-54-05_000` | 1086 | 9.58 | 23.72 | 2.48 | 2.73 | 232–414 / 460–651 |
| `..._15-14-26_001` | 882 | 7.30 | 23.36 | **3.20** | **6.51** | 1–141 / 618–648 |

（`15-14-26_000` 由另一路 agent 标注，我独立算出 53–292 / 824–900，与之吻合 —— 两次独立标注互为验证。）

**判据验证**：这 4 条里 3 条同时满足 §7 的四条判据，中位/p90/对比度与标杆 `caicai-003`
（6.87 / 21.93 / 3.19）几乎重合。说明「固定机位 + 有聊天/待机间隔」的抖音舞蹈主播
**是能被判据捞出来的**，之前的失败是主播形态不对，不是判据无效。

### 11.2 ✅ 训练结果：技术目标（F1 ≥ 0.80）达成

`ablate_norm.py`，真留一切片，13 切片 / 6744 行：

| 配置 | 原 10 片 | 本次 13 片 |
| --- | --- | --- |
| A 现状 `0.8/0.2 th1.5` | 0.584 | 0.583 |
| B 主播历史基线（线上权重） | 0.614 | **0.697** |
| C 折内搜权重/阈值（切片内归一化） | 0.704 | **0.822** |
| D 折内搜（历史基线） | 0.695 | 0.812 |
| E 逻辑回归 5 特征 | 0.604 | 0.790 |
| **落地候选 `1.0/0.0 th1.2`** | 0.717 | **0.831** |
| 折内最优 | 0.711 | 0.829 |

- **召回率 0.438 → 0.830** —— 短板（漏检）被补上了，这是这次最大的变化。
- **`1.0/0.0/th1.2` 与折内最优（0.829）几乎相同** → 手选值没有过拟合空间，可以放心上线。
- 阈值最优仍稳在 **th≈1.2**（0.831），第四次独立复现。
- **`B 主播历史基线` 第一次真起作用**：0.614 → **0.697**。原因很实在 ——
  `菜菜很忙` 现在有 4 条切片（原来只有 1 条），§4.2 说的"8/10 冷启动"问题缓解了。
  **§11.7 的判断是对的：不是方法无效，是之前没历史可用。**
- ⚠️ 一处要修正：折内搜出的**音频权重这次是 +0.11/+0.15（微正）**，不再是负的。
  §4.3"音频该为 0"的结论在新数据下弱化了 —— 但它只占运动权重的 8%，实际仍是"可忽略"。
  **建议维持 audio_weight=0，但把它当作"不重要"而非"方向相反"。**
- 逻辑回归 0.790 仍低于纯配置 0.831 → **Phase 3 继续暂缓，判断不变**。

### 11.3 ⚠️ 最重要的发现：`15-14-26_001` 是缺陷 D/E 的活教材

这条切片按 z 分排出的前 3 个候选里，**有 2 个是假的**：

| 候选 | z 峰值 | 画面真相 |
| --- | --- | --- |
| 00:01–02:21 | 3.77 | ✅ 全身景跳舞（抬腿/转身/比心） |
| 10:23–11:09 | 4.60 | ❌ **10:50 机位切近景特写**（脸占满画面）→ 缺陷 E |
| 11:34–12:30 | **6.51** | ❌ **全屏旋转木马礼物动画** → 缺陷 D |

拼图证据：`_diag/train/sheets_live/cc001_candA / candB / spike / norm`。

**这条把任务的根本困难摆到了台面上**：

1. **缺陷 E（机位切换）不只是"SOOP 那种切景别"的问题** —— 抖音主播会**主动**切近景互动，
   而切近景时z分反而升高。**运动量与"是否在跳舞"在切机位时是负相关的。**
2. **缺陷 D（礼物特效）比想象的更普遍**：它出现在**固定机位的舞蹈直播间**里，
   而且能刷到全场最高 z（6.51 > 真实跳舞的 3.77）。**这说明它污染的不只是"检测"，
   还污染"标注"—— 如果不看图，这段的全屏动画会被当成最强高光。**

→ **推论：`运动量` 这一维已经被证伪到接近极限。要真正做"跳舞高光"，
必须有能区分「整屏变化」与「身体局部动作」、以及「尺度不变」的特征。**
这两条都不是调参能解决的（§9 缺陷 D/E）。

### 11.4 下一步（按收益排序）

1. **继续攒 `菜菜很忙` 这类主播的切片**（她还在播，每 15 分钟滚一片）。
   同一主播样本越多，"主播历史基线"收益越明显 —— 这是当前**唯一确定能涨**的方向。
2. **把礼物特效/机位切换做成独立的负样本标记**，而不是混在 positive 之外不管：
   现在它们只是"没标为高光"，但特征上它们是最高分。应该显式记录这些区间，
   供将来做"假高光排除"使用。
3. **空间分块特征**（缺陷 D/E 的共同正解）：需要先修 `probe.go` 的
   `Parsed_metadata_(\d+)_` 实例序号解析。中等改动，但这是目前唯一能突破天花板的改动。
4. 判据补充：`中位运动量 ∈ [4,12]` + `候选密度 < 0.15/分`（§10.4），
   配合已有的四条，一起作为采集侧粗筛。

---

## 12. 继续做训练：误差定位 + 一个关键否定结论

### 12.1 🎯 逐切片分解 —— 在「跳舞」口径下其实已经 0.92

同样的 `1.0/0.0/th1.2`，按切片拆开看（第 3 列起是误检/漏检秒数）：

| 切片 | 场景 | P | R | F1 |
| --- | --- | --- | --- | --- |
| `caicai-003` | douyin-dance | 0.962 | 0.971 | **0.967** |
| `..._14-54-05_000` | douyin-dance | 0.994 | 0.946 | **0.970** |
| `..._15-14-26_000` | douyin-dance | 0.904 | **1.000** | **0.950** |
| `..._15-14-26_001` | douyin-dance | 0.636 | 0.924 | **0.753** |
| `dance-064016` | soop-perf | 0.901 | 0.869 | 0.885 |
| `dance-064128` | soop-perf | 1.000 | 0.609 | 0.757 |
| `dance-064057` | soop-perf | 0.769 | 0.571 | 0.656 |
| `gorani-10` | soop-perf | 0.811 | 0.484 | 0.606 |
| `dance-064023` | soop-perf | 0.684 | 0.406 | 0.510 |
| `golani-2` | soop-perf | 0.514 | 0.486 | 0.500 |
| `dance-224903` | soop-perf | 0.517 | 0.385 | 0.441 |
| `dance-224222` | soop-perf | 0.440 | 0.436 | 0.438 |
| **合计** | | 0.833 | 0.830 | **0.831** |

**按目标口径分层（只算 4 条 douyin-dance）：P 0.882 / R 0.962 / F1 0.920。**

⚠️ **聚合的 0.831 是被 9 条 SOOP 拖累的**，而那批素材按 §4.5 本身就是"整片都在表演、没有常态参照"，
**不属于用户要的「跳舞高光」口径**。继续把它们混在一起报，会把跳舞这边的进展完全埋掉。

→ **建议：以后评估必须先按 `scene` 分层报告，再给聚合值。**

### 12.2 误检不是"到处都有"，而是"一个状态没被识别"

`15-14-26_001` 有 90 秒误检，逐秒归因：

| 误检区间 | 秒数 | 归因 |
| --- | --- | --- |
| 11:05–11:09 / 11:34–11:47 / 11:58–12:30 / 13:12–13:29 / 14:02–14:36 | **85s（94%）** | **全在 10:55–15:00 这一整段**：机位切近景特写 + 全屏礼物动画 + 三方连麦画面切换 |
| 02:21 / 06:21–06:23 / 07:13–07:16 | 8s | 常态段里的零散抖动 |

10:55–15:00 这一段我**逐帧看过**（拼图 `cc001_unlabeled_1130-1305` / `_1308-1448`）：
13:08 起是近景特写、14:12 是**三方连麦分屏**、还挂着"还未开播"字幕 —— **语义上完全不是跳舞**。

→ **只要能把这一段判成"非跳舞态"**：该切片 F1 `0.753 → ≈0.94`，
跳舞分层 F1 `0.920 → ≈0.96`。**这是目前最大的一块可摘果实。**

### 12.3 ❌ 关键否定：现有 50 列**无法**判断「机位是全身景还是近景」

在同一段视频、同一主播内做对照（最干净的设定：机位、光照、人物都恒定，差异只来自状态）：

| 列 | 全身景 | 近景组 | 效应量 |
| --- | --- | --- | --- |
| `v_HUEMED` | 43.97 | 44.37 | 1.07 |
| `v_HUEAVG` | 43.95 | 44.30 | 1.03 |
| `v_ULOW` | 0.537 | 0.141 | 0.81 |
| `a_Peak_count` | 138 | 707 | 0.25 |

最好的 `v_HUEMED` **只差 0.4 度色相**，是靠"组内方差极小"撑出来的 —— 换个主播/换盏灯就失效，
**它不是几何特征**。`a_Peak_count` 虽然差 5 倍（138→707，近景说话时峰值密），但组内方差太大。

→ **结论：现有特征是"整图全局标量"，结构上无法表达「尺度/构图」。**
**要修缺陷 E（以及缺陷 D 的整屏变化），必须加空间信息 —— 没有别的路。**
这是下一步动 `probe.go` 的**唯一依据**，也是本节最硬的结论。

### 12.4 留个记录：一个我自己提出又否掉的假设

我一度以为 `a_Flat_factor` 是"真跳舞"的判别列（某窗口跳舞 0.256 vs 其余 0.000，效应量 14.5）。
复查发现它**全片 98.2% 是 0**，尖峰出现在 10:00 和 14:00，**与跳舞窗口并不对齐** ——
那个 0.256 是我把 `dance1` 和 `dance2` 两段窗口取平均取出来的假象。

**已否掉，不要再用它。** 教训与方案 §5 一致：**先看单列的时间序列，再信任何"均值差"。**

### 12.5 下一步（收敛成一条）

误差已经定位到「**状态识别**」而不是「阈值/权重」——阈值已经第四次复现为 th≈1.2，没有挖头了。

**唯一能突破 0.92 → 0.96+ 的改动**：在 `probe.go` / `features.go` 加**空间分块特征**
（前提是先修 `Parsed_metadata_(\d+)_` 实例序号解析，见 §4.1 的坑），用来表达两件事：

1. **整屏变化 vs 局部动作** → 挡礼物特效/切场景（缺陷 D）
2. **人物占画面的比例（尺度）** → 挡切近景（缺陷 E），并让"动作幅度"在远景下也能算对

在此之前，继续攒 `菜菜很忙` 这类主播的切片仍是**稳妥的收益来源**（§11.2 已证明：同主播样本从 1 条增到 4 条，
"主播历史基线"就从 0.614 涨到 0.697）。

**标注侧新增**：`labels.jsonl` 开始记录 `negative_hard` 字段（已验证的"高运动但非跳舞"区间），
本次记录了 3 段共 245 秒。当前训练脚本**还不消费**这个字段，它是为将来的"假高光排除"留的。

---

## 13. ✅ 找到了一个能落地的改动：加长平滑窗口（零新特征）

### 13.1 先修正 §12.2 里的一个机制判断

我原以为缺陷 D（礼物特效）是"持续十几秒的段"（方案 §11.11 在游戏切页上量到的是这样）。
**在抖音礼物特效上不是** —— 它是 **2~4 秒的极端尖峰**：

| 窗口 | change_ratio（画面变化像素占比） |
| --- | --- |
| 单独取 12:25–12:45 | **0.88** ✅ 确实几乎全屏在变 |
| 但整段 12:24–12:48 平均 | 只有 0.241 |

而真跳舞是**持续抬升**（0.491）。**"尖峰 vs 持续"这个差别是可以被平滑窗口利用的。**

（顺带把一个新算出来的指标否掉：`change_ratio` 这个"变化像素占比"列**不能**用来区分假高光 ——
真跳舞反而是最高的 0.491，切近景 0.353、礼物 0.241、近景连麦 0.274。又一条被否掉的假设。）

### 13.2 实验：平滑窗口 × 阈值

`score.go:robustZ` 现在的 `SmoothWindow = 5s`。注意：**平滑窗口一变，z 的尺度就变了**
（滑窗越长 → MAD 越小 → z 越大），所以窗口和阈值**必须一起调**。

| 平滑窗口 | 最好阈值 | 跳舞 4 条 F1 | 误检秒 | 漏检秒 | 全部 13 条 F1 |
| --- | --- | --- | --- | --- | --- |
| **5（线上现状）** | 1.2 | 0.922 | 131 | 39 | **0.831** |
| 9 | 1.5 | 0.938 | 105 | 30 | 0.826 |
| **15** | **1.8** | **0.945** | **82** | 36 | 0.822 |
| 21 | 2.2 | 0.942 | 64 | 57 | 0.820 |
| 31 | 2.2 | 0.938 | 59 | 70 | 0.795 |

### 13.3 留一验证（关键，否则就是过拟合）

在同一批数据上扫 (窗口, 阈值) 然后报最好值，等于在测试集上挑参数。
所以对 4 条跳舞切片做了**留一**：每折只用其余 3 条搜 (窗口, 阈值)，再测被留出的那条。

| | P | R | **F1** | 误检秒 | 漏检秒 |
| --- | --- | --- | --- | --- | --- |
| 留一（每折自选窗口/阈值） | 0.941 | 0.956 | **0.949** | 62 | 46 |
| 对照：线上 `w=5, th=1.2` | 0.922（F1） | — | **0.922** | 131 | 39 |

**结论：跳舞口径 F1 0.922 → 0.949（+2.7），误检从 131 秒腰斩到 62 秒。**
而且这是**只改两个数**（`SmoothWindow` 5→15、`highlight_threshold` 1.2→1.8），
**不需要任何新特征** —— 比 §12.5 那条"加空间分块特征"的路便宜一个数量级。

### 13.4 ⚠️ 为什么**先不要上线**

1. **验证基础太薄**：只有 **4 条**跳舞切片、1 个主播。留一在 4 折上的方差很大
   （逐折 F1 是 0.980 / 0.976 / 0.987 / **0.787**，最后那条就是 `15-14-26_001`）。
2. **聚合指标会略降**（0.831 → 0.822），因为 SOOP 那批偏好短平滑。虽然 SOOP 不在目标口径内，
   但它是在线的真实流量，改动前要意识到这个取舍。
3. **`SmoothWindow` 目前是 `score.go` 里的常量**，不是配置项 → 要上线得改代码。
   按方案自己的约定（§8「不改旧字段名」），应该**新增** `highlight_smooth_window` 字段、
   默认值保持 5（行为不变），确认后再改配置值。

**建议的推进顺序**：再攒到 **≥10 条同形态跳舞切片**（`菜菜很忙` 还在播，每 15 分钟一片）→
重跑这个留一 → 若仍 ≥0.94，再改代码加配置项 → 灰度上线。

### 13.5 本轮小结

| 结论 | 性质 |
| --- | --- |
| 跳舞口径分层 F1 = 0.920（聚合 0.831 被 SOOP 拖累） | ✅ 已确认 |
| 误检 94% 集中在"切近景/礼物/连麦"一个状态 | ✅ 已确认 |
| 现有 50 列无法判断「全身景 vs 近景」 | ❌ 否定（要加空间特征） |
| `change_ratio`（变化像素占比）能挡假高光 | ❌ 否定 |
| `a_Flat_factor` 是跳舞判别列 | ❌ 否定（自己提的，已撤回） |
| **加长平滑窗口 5→15 + 阈值 1.8，跳舞 F1 0.922→0.949** | ✅ 留一验证通过，待更多数据复核 |

---

## 14. 新需求：「露肤」（低胸装 / 露腿）也算高光 —— 技术评估

> 用户 2026-09-23 16:05 追加要求。**本节只做技术评估，尚未实现。**

### 14.1 ⚠️ 它和「跳舞」是两个正交的检测目标，不能塞进现有链路

| | 跳舞高光 | 露肤高光 |
| --- | --- | --- |
| 判据性质 | **时序运动**（这一秒画面在不在大幅变化） | **单帧外观**（这一帧画面长什么样） |
| 现有 50 列能否用 | ✅ 可用（`v_YAVG` 等） | ❌ **完全无用** —— 帧差在静止画面上恒为 0，穿什么衣服都不影响 |
| 时序逻辑 | 有明确起止（动作开始/结束） | 是**状态**而非事件，可能整场都成立 |

**结论：必须新增一条独立的检测支路，不能并入 `score.go` 的运动量打分。**

### 14.2 硬件约束下的可行方案（J1900 + 2G，禁 CNN，见方案 §1.3）

可行的是**经典肤色比例检测**，纯像素统计、无模型：

1. 帧转 YCbCr，用经典肤色阈值（如 `77≤Cb≤127` 且 `133≤Cr≤173`）判肤色像素
2. **关键在分区域**，不能算全图比例：
   - 胸部区（画面中上部）：肤色占比 → 对应"低胸装"
   - 腿部区（画面下部）：肤色占比 → 对应"露腿"
3. 输出两列标量 `skin_chest_ratio` / `skin_leg_ratio`，在 160×90 缩略图上算
   → **成本几乎为零**，与方案 §1.3「已付一次全解码的代价，加特征几乎免费」同一个道理

### 14.3 ⚠️ 四个必须提前知道的坑

1. **抖音主播普遍开美颜+补光灯，肤色会被推成"橙白"**，公开阈值基本不能直接用，
   必须按平台/按主播标定。另外**白墙、木地板、暖色灯光、奶茶色桌面**都会产生大量假阳性。
2. **"低胸装"往往是整场的基线状态，不是高光。** 这条有实证：我刚看过 `莉莉^^` 的抽帧
   （`sheets_live/li_0900-1200_low.jpg`），**她全场都穿蕾丝低胸吊带**。
   按"露肤比例高 = 高光"的规则，**她整场 15 分钟都是高光**，等于没有筛选。
   → 判据必须是**相对变化**（比该切片/该主播自己的基线更露），而不是绝对比例。
   这与 §4.5「整片都在表演」是同一类失败模式。
3. **它是静态属性，所以现有的"阈值切分 → 合并 → 丢短 → 限长 → 补边"整条时序逻辑不适用**，
   要另设计（例如只在"比例相对基线抬升且持续 N 秒"时才切）。
4. **需要独立标注与独立指标**：`labels.jsonl` 加字段（如 `exposure:[[a,b],...]`），
   **指标单独报**，绝不能和跳舞 F1 混在一起 —— 否则两个目标的错误互相掩盖
   （§12.1 已经吃过一次"混在一起报会埋掉进展"的亏）。

### 14.4 建议的落地顺序（先验证可行性，再谈检测）

1. 在 `features.go` 加 `skin_chest_ratio` / `skin_leg_ratio` 两列（成本极低，可随时回退）
2. 在全 55 个已提特征切片上重提一遍，**先看这两列的分布**：
   能不能把主播分成"一直露"和"偶尔露"两类？
3. 若能分开，再定"相对基线抬升"的判据；若不能（例如美颜把所有人都推成高肤色），
   **就说明这条路在纯像素统计下走不通**，需要重新评估成本
4. 全程不进主链路，先在 `cmd/hleval` / `tools/` 里验证

---

## 15. 进行中：把 §13 的平滑窗口做成配置项

`highlight.Options` **已经有 `SmoothWindow` 字段**（`score.go:96` 用它调 `robustZ`），
所以不需要改 `score.go`。剩下三步：

1. `internal/config/config.go`：新增 `HighlightSmoothWindow int` / `json:"highlight_smooth_window"`
2. `applyDefaults`：`<=0` 时回落 5（**保持现有行为不变**）
3. `internal/app/highlight.go: highlightOptions`：`if b.HighlightSmoothWindow > 0 { o.SmoothWindow = ... }`

⚠️ 必须**默认 5**，这样上线前行为完全不变，等 §13.3 的验证在更多切片上复现后，
再改配置值 + 同步调 `highlight_threshold`（两者必须一起动）。**本轮尚未动代码。**

---

## 16. 素材来源边界（2026-09-23 16:06 记录）

用户提出 `D:\学习资料` 里的短视频也可作训练素材。只读扫描结果：

| 位置 | 文件数 | 大小 | 判定 |
| --- | --- | --- | --- |
| `D:\学习资料` 根目录（约 36 个直放文件） | — | 数 GB | ⛔ **成人色情内容，不使用** |
| `D:\学习资料\抖音下载` | 376 | 748 MB | ✅ 可用（哈希命名，抖音短视频） |
| `D:\学习资料\抖音爬取` | 473 | 885 MB | ✅ 可用（同上） |
| 合计视频 | 885 | 11 GB | |

**根目录那批不用于训练，理由（技术 + 项目约定，不是价值判断）：**

1. **分布完全不同**：线上录制的是**抖音/SOOP 公开直播**。成人内容和直播切片在画面构图、
   机位、时长、场景结构上没有可比性，拿它训练只会让检测器在真实场景上变差。
2. **项目自己的约定**：§4.5 的 `dance-a3d6` 已经被标为「⛔ 成人内容，已排除」。
   再把它当训练素材是自相矛盾的。
3. 该目录在工作区之外，本文件只做只读清单记录，**不索引、不移动、不复制、不处理**。

**`抖音下载` / `抖音爬取` 这 849 个文件（1.6 GB）的正当用途，按 §11.8 的既有原则：**

- ❌ **不能进跳舞训练集** —— 它们是**已剪辑的短视频**，没有"常态段"，与"长直播里找舞蹈段"
  是不同分布（§11.8 早已确立，当时只用 58 条做量级普查）。
- ✅ **可以用于两条**：
  1. **露肤支路的可行性验证**（§14.4 第 2 步）—— 露肤是**单帧外观**特征，
     不需要时序结构，短视频完全适用。这是这批素材最合适的用途。
  2. **量级普查**：中位运动量/肤色比例的跨主播分布，用来定 §7 判据的阈值区间。
- ⚠️ 抽样时若发现其中也含成人内容，立即停止并单独隔离，不混入任何统计。

---

## 17. ❌ 露肤检测的可行性验证：**纯像素肤色阈值走不通**（已实测否掉）

> §14 我提的方案是"分区域肤色比例"。**已实装探针并实测，结论是否定的。**
> 工具：`_diag/train/skin_probe.py`（一次解码算脸区/胸区/腿区三块肤色占比）。
> 这轮只花了一小时，比写完检测器再发现便宜得多 —— 这就是先验证的价值。

### 17.1 先说看起来很有希望的那部分

**同一主播内**，胸区信号确实不是"脸大"的代理：

| `莉莉^^` 窗口 | 脸区 | 胸区 | 腿区 |
| --- | --- | --- | --- |
| 300–420s | 0.521 | **0.684** | 0.279 |
| 600–700s | 0.191 | **0.680** | 0.262 |

脸区从 0.52 掉到 0.19，胸区稳在 0.68。对照 `菜菜很忙`（女仆装）胸区只有 **0.072 / 0.096**。
**光看这两个人，差了 7~9 倍，像是成了。**

### 17.2 但跨主播一看就崩了

| 主播 | 服装（均已抽帧看过） | 脸区 | **胸区** | 腿区 |
| --- | --- | --- | --- | --- |
| `菜菜很忙` | 女仆装，包得严 | 0.04–0.16 | **0.07–0.25** | 0.10–0.14 |
| `莉莉^^` | **低胸吊带** | 0.19–0.52 | 0.68 | 0.26–0.28 |
| `乔乔吃不饱` | 粉色女仆装 | 0.71–0.97 | **0.61–0.72** | 0.07–0.20 |
| `半枝桃` | 正常服装 + 扇子 | 0.87–0.98 | **0.42–0.67** | 0.63–0.68 |
| `FISH` | 正常服装，静坐 | 0.02–0.63 | **0.73–0.86** | 0.32–0.92 |
| `yuan🐱` | 正常服装 | 0.71–0.83 | **0.78–0.86** | 0.93–0.97 |

**6 个主播里 5 个胸区都在 0.6–0.9 —— 包括穿女仆装的 `乔乔吃不饱`（0.61–0.72）
和正常穿衣的 `yuan🐱`（0.78–0.86）。只有 `菜菜很忙` 一个落在低区。**

→ **指标没有判别力。它测的不是衣服，是"画面里有多少肤色系像素"**：
暖色墙面、木地板、肤色系家具、补光灯下的整体偏暖画面，全部落在经典 YCbCr 肤色区间内。
§14.3 预警的坑 1（美颜/补光 + 白墙/木地板假阳性）**被实测证实了，而且比预想的更严重 ——
它不是"有一些假阳性"，而是"背景信号直接盖过衣物信号"。**

### 17.3 结论与唯一还值得试的方向

**❌ 直接否掉**：固定区域 + 绝对肤色比例阈值。跨主播无判别力，这条路不用再花时间。

**⚠️ 还有一个便宜的方向没试，但它和缺陷 D/E 的正解是同一个改动**：

> **先定位"人在哪"，再在人的范围内测肤色。**

理由很直接 —— 现在胸区占比 0.7 的那些切片，很大一部分框到的是**背景**而不是胸口。
如果能把测量限制在**人所在的分块**上，背景就被排除掉了。

而"人在哪"正好可以用**分块帧差**来估：运动集中在哪几个块，人就在哪。
于是：

| 需求 | 共同依赖 |
| --- | --- |
| 缺陷 D（礼物特效=整屏变化） | **分块空间特征** |
| 缺陷 E（切近景=尺度变化） | **分块空间特征** |
| 露肤检测（排除背景） | **分块空间特征 + 在"人块"内测肤色** |

**三件事收敛到同一个改动上** —— 这也把"该不该动 `probe.go`"的回答从"要动"变成了
"要动，而且一次能解决三个问题"。前提仍然是先修 `Parsed_metadata_(\d+)_` 实例序号解析（§4.1 的坑）。

### 17.4 顺带记一个实现坑

`metadata=print:file=<路径>` 的路径**不能含盘符冒号**（`:` 是 ffmpeg filter 的选项分隔符），
写绝对路径会报 `Error : Invalid argument`。用**相对路径**即可。
另外 Python 文档字符串里别写 Windows 反斜杠路径（`\U` 会被当 Unicode 转义，SyntaxError）。
`skin_probe.py` 里两处都已注明。

---

## 18. 留一复现尝试：新切片**不可用**（金标准主播也有"未开播/连麦"状态）

`菜菜很忙🦋_2026-09-23_15-58-35_000`（901s）攒到了，但**不能用来复现 §13.3 的留一**：

| 指标 | 值 | 判据 |
| --- | --- | --- |
| 中位运动量 | 4.58 | ✅ ∈[4,12] |
| p90 | 8.79 | ❌ 需 >20 |
| 对比度 | 1.92 | ❌ 需 >2 |
| z 峰值 | **11.80** | ⚠️ 原始值只有 13.5 → 典型「低基线 z 虚高」 |

抽帧一看就明白（`sheets_live/cc5800_spike_0050-0210.jpg`）：
**画面右上角挂着「三方连麦中，还未开播」字幕**，她在做轻微摆姿势，
00:01:20 处切成连麦分屏画面 —— **那个 z=11.80 的尖峰来自分屏布局切换，不是跳舞。**

两条结论：

1. **同一主播的视频流有多个状态**：`未开播/连麦` → `表演` → `聊天`。
   只有 `表演` 状态有训练价值，而**连麦布局切换会产生全场最高的 z**（又一次缺陷 D/E）。
2. **§13.3 的留一复现还得再等** —— 需要的是"已开播 + 固定机位全身景 + 真跳舞"的切片，
   而不是"某个主播的任何一片"。这把 §7 判据的必要性又推高了一档：
   **判据不只是筛素材，还得先筛"开播状态"。**

⚠️ 运营提醒：该连麦画面里出现了**明显未成年的参与者**。自动化剪辑产出这类片段有合规风险，
建议在采集/产出侧对"连麦"状态单独处理。

---

## 19. 修掉「只传高光」的两个边角 bug（2026-09-23 16:24，代码已落地，未部署）

用户问"高光上传开启之后截图不会传到云端吗"→ 核查属实，顺手修了两个问题。

### Bug 1：截图归档被「只传高光」误拦，永远到不了云端
- 触发开关是 `highlight_only_upload`（UI「高光上传」），**不是** `highlight_enable` 总开关。
- 链路：`app.HandleFile` 入口 `shouldSkipUpload` → `matchHighlightOnlyPrefix`，判据只有
  "在 `<主播>/` 下且相对路径不含 `高光/`"；而截图归档在 `<主播>/<日期>/Screenshots/`
  （`builtin_ffmpeg.go` 抽帧协程落盘）→ 被当原片跳过。旧单测 `TestMatchHighlightOnlyPrefix`
  没覆盖这个组合。
- 修复：`matchHighlightOnlyPrefix` 给 `Screenshots/` 加白名单（recorder 新增
  `ScreenshotDirName` 常量，录制落盘与上传过滤共用同一来源）。

### Bug 2：「只传高光」模式下原片分析完不删，永久占盘
- 根因是**删源责任链断头**：原片被 `shouldSkipUpload` 拦下后从不进入上传管线 →
  pipeline 的 `BeforeRemove`（`highlightClaim`）永远不会被调 → `highlightClaimed` 里
  没有它 → 旧的 `highlightReleaseClaim` 只删"被认领"的 → 原片永远留在本地。
  （「原片也上传」模式没这个问题：pipeline 传完认领，高光分析完删。）
- 修复：`highlightReleaseClaim` 改名为 `highlightDisposeSource`，删源判定抽成纯函数
  `highlightShouldDeleteSource(path, claimed, concluded, onlyPrefixes)`：
  1. 认领过的必删（维持旧行为）；
  2. 「只传高光」主播的原片：**已有定论**（已产出 / 未检出 / 失败达重试上限）才删，
     失败未达上限的留着等下一轮 —— 否则重试机制形同虚设；
  3. 「原片也上传」的未认领文件绝不动（防高光抢跑删掉待传原片）。
  2 和 3 的"只传高光"判定复用上传过滤同一套口径，保证"被跳过上传的"和"被删掉的"
  是同一批文件。

### 验证与配套
- `go build ./...` + `go vet` + `go test ./... -count=1`：19 包全绿（含新增
  `TestHighlightShouldDeleteSource` 6 用例与截图放行用例）。
- UI 文案同步：`web/index.html` 两处「原片保留在本地」改为「分析完即删」口径，
  并把过期的高光产物路径说明（`主播目录/高光` → `主播目录/<日期>/高光`）一并修正。

### ✅ 已部署上线（2026-09-23 18:06）
- 本地交叉构建 linux/amd64 静态二进制（CGO_ENABLED=0，14M，web/index.html 是
  go:embed 内嵌的所以 UI 改动随二进制一起上线）。
- 传输踩坑：**Windows 的 OpenSSH scp 在这条 Clash 中转链路上必挂**
  （`Connection closed by UNKNOWN port 65535`，重试也没用）；换成
  `ssh xyx.homes "cat > 目标" < 本地文件` 的 **stdin 管道**一次就过。
  另外老 gzip（CentOS 7）没有 `-k`，用 `gzip -dc xx.gz > out` 代替。
  5.2MB 的 gz 传一次约 1 分钟，链路 flaky 时包重试循环。
- 动作：备份旧版到 `/home/upload/backup-deploy-hlfix-20260923-1657/uploader`
  （md5 cb87a129...）→ md5 校验新版（694c9f50...）→ stop/替换/start →
  服务 active、`curl 127.0.0.1:8888` HTTP 200、监控全部恢复
  （菜菜很忙🦋 18:06:35 恢复录制，录屏+截屏双开、TG bot 接管正常）。
- 本次上线内容 = §19 两个修复 + §13/§15 的 `highlight_smooth_window` 配置项
  （线上 config.json 没配该字段，回落 5，行为不变，符合 §13"先不上调参"的决定）。

---

## 19. 分块空间特征：**逐秒对照后否掉**（不写进 probe.go）

### 19.1 先说结论

**三分带（上/中/下）方案不成立，已否掉，没有写进 `probe.go`。**
工具留在 `_diag/train/band_probe.py`（长窗口统计）+ `band_eval.py`（逐秒 AUC 对照），可复现。

### 19.2 长窗口平均：完全分不开

`band_probe.py`，一次解码 split 成 1 全帧 + 3 分段，各自写独立 metadata 文件
（`metadata=print:file=` 是每个实例各自的，所以 key 同名也不串台 —— 这正是 §4.1 那个坑的正解）。

| 窗口（`15-14-26_001`） | 全帧 | 上 | 中 | 下 | 局部度 | 上/(中+下) |
| --- | --- | --- | --- | --- | --- | --- |
| 1–141 **跳舞** | 20.7 | 14.5 | 28.7 | 19.0 | 1.38 | 0.61 |
| 618–648 **跳舞** | 18.1 | 10.6 | 21.3 | 22.5 | 1.24 | 0.48 |
| 655–715 **切近景** | 14.4 | 8.9 | 18.4 | 16.0 | 1.27 | 0.52 |
| 744–768 **礼物特效** | 9.1 | 4.9 | 12.0 | 10.3 | 1.33 | 0.44 |
| 788–881 **连麦** | 11.1 | 7.3 | 14.7 | 11.4 | 1.32 | 0.56 |

`局部度`（三段 max/mean）：跳舞 1.24–1.38，假高光 1.27–1.33，**完全重叠**。
`上/(中+下)`：跳舞 0.48–0.61 vs 假高光 0.44–0.56，**也重叠**。

### 19.3 逐秒对照（关键，别被长窗口骗了）

假高光是**短尖峰**，长窗口平均会把它抹平。所以按**秒**、只取"全帧运动量>3"的秒来比：

`15-14-26_001`（跳舞 166 秒 vs 假高光 203 秒）：

| 特征 | 跳舞中位 | 假高光中位 | AUC |
| --- | --- | --- | --- |
| 全帧（现有指标） | 20.5 | 11.5 | 0.740 |
| 局部度 | 1.395 | 1.304 | 0.664 |
| 中/全帧 | 1.383 | 1.267 | 0.684 |
| 下/全帧 | 0.914 | 1.040 | 0.342 |
| 上/全帧 | 0.690 | 0.690 | 0.498 |

`14-54-05_000`（跳舞 373 秒 vs 常态 285 秒）做同向检验：

| 特征 | 跳舞中位 | 假高光中位 | AUC |
| --- | --- | --- | --- |
| 全帧 | 21.5 | 7.0 | **0.994** |
| 局部度 | 1.377 | 1.543 | 0.204 |
| 中/全帧 | 1.378 | 1.545 | 0.244 |
| 下/全帧 | 0.939 | 0.556 | 0.864 |

**四个派生特征在两个切片上方向全部相反**（局部度 0.664 vs 0.204、中/全帧 0.684 vs 0.244、
下/全帧 0.342 vs 0.864）。**唯一稳健的还是 `全帧` 本身（0.740 / 0.994）—— 也就是现有指标。**

→ 纵向三分带**表达不了"尺度"**：近景时人脸的小幅运动并不会集中到上段，
全身景跳舞时手臂也横扫三段。**这条路不用再花时间。**

### 19.4 那缺陷 D/E 还剩什么路

纵向三分带失败后，还剩下的都是更重的改动：

1. **二维分块**（3×3 或 4×4，而不是只纵向切三条），并且**不要只取 max/mean，
   而是把 9~16 个块的运动量作为独立特征喂进模型** —— 空间分布的信息在"哪些块"上，
   不在"最大块/平均"这个标量里。
2. **块间相关性**：整屏变化时各块高度同步（相关≈1），局部动作时只有相邻块同步。
   这比 min/max 统计更贴近"整屏 vs 局部"的定义。
3. **两条都要求先把"每块的运动量"变成可用的特征列**，也就是回到 §4.1 那件事：
   修 `Parsed_metadata_(\d+)_` 解析 + 多路实例。
   **但注意**：本节实测证明"修好解析"本身不带来收益 —— 收益取决于选对**块粒度和统计量**，
   而目前两版（三分带、min/max）都被否了。所以动 probe.go 之前，
   **应该先在 `band_probe` 这类探针里把"块粒度 × 统计量"试出来**，别再直接改主链路。

---

## 20. 部署状态：**未完成，链路不通**（2026-09-23 16:35）

### 20.1 已完成

| 项 | 状态 |
| --- | --- |
| 平滑窗口配置化代码 | ✅ `config.go` 加字段 + `applyDefaults` 回落 5；`highlight.go` 映射；新增单测 |
| 本地验证 | ✅ `go build ./...` 通过；`go test ./...` **0 失败** |
| Linux 二进制 | ✅ `release/uploader`（14.5 MB，`GOOS=linux CGO_ENABLED=0`）<br>md5 `f4bc9b1dd1719a9948481c581b685fa5`<br>已确认新字段编译进去（`grep -a highlight_smooth_window` 命中 1） |

### 20.2 卡在传输

| 方式 | 结果 |
| --- | --- |
| `scp` | ❌ `Couldn't send packet: Broken pipe` / `Connection closed`（6 次全败）<br>——它的全双工通道过不了 Clash/WARP 中转 |
| `ssh "cat > f"` 管道 | ❌ `Timeout, server not responding` / `Connection closed`（5 次全败） |
| 之后 `ssh` 本身 | ❌ 连 `hostname` 都失败（8 次探测、约 80 秒全败）—— **大文件传输把隧道搞降级了** |

**服务器上什么都没改**：旧二进制仍在跑，**config.json 一个字节没动**。
⚠️ 待清理：失败的上传可能在服务器留下一个 `/home/upload/uploader.new`（残缺文件），
链路恢复后应先 `ls -la` 确认并删除。

### 20.3 恢复后的部署步骤（按这个顺序做）

```bash
# 0) 先清理残缺文件
ssh xyx.homes "ls -la /home/upload/uploader.new && rm -f /home/upload/uploader.new"

# 1) 传输（scp 走不通，用管道；若仍失败则改传 diff + 服务器上编译）
ssh xyx.homes "cat > /home/upload/uploader.new" < release/uploader
ssh xyx.homes "md5sum /home/upload/uploader.new"      # 应为 f4bc9b1dd1719a9948481c581b685fa5

# 2) 备份 + 换 + 改配置 + 重启（一次会话内完成）
ssh xyx.homes 'bash -s' <<'EOF'
set -e
cd /home/upload
TS=$(date +%Y%m%d-%H%M%S); mkdir -p "backup-deploy-hl2-$TS"
cp -a uploader config.json "backup-deploy-hl2-$TS/"
systemctl stop uploader
mv uploader.new uploader && chmod +x uploader
sed -i -e 's/\("highlight_smooth_window":[[:space:]]*\)[0-9]*/\115/' config.json
# 若字段不存在（旧 config.json 没有这一行），改用 python 插入到 "highlight_merge_gap" 之后
sed -i -e 's/\("highlight_threshold":[[:space:]]*\)1\.2/\11.8/' config.json
systemctl start uploader
sleep 12
systemctl is-active uploader && ps -eo args | grep -c '[f]fmpeg'
EOF
```

⚠️ 注意：**`highlight_smooth_window` 与 `highlight_threshold` 必须一起改**（5→15 配 1.2→1.8）。
⚠️ 若旧 config.json 里没有 `highlight_smooth_window` 这一行，`sed` 不会生效，
必须用 python 在 `highlight_merge_gap` 后面插入 —— 这一条上次没考虑到。

### 20.4 部署前的取舍（再确认一次）

| 数据集 | `w=5, th=1.2`（现状） | `w=15, th=1.8` |
| --- | --- | --- |
| 跳舞 4 条（留一验证） | 0.922 | **0.949**（误检 131→62 秒） |
| 全部 13 条混合 | **0.831** | 0.822 |

**目标口径（跳舞）明显改善，混合口径略降**（SOOP 那批偏好短平滑）。
配置改一行就能回退，`backup-deploy-hl2-*` 里也留了原文件。
